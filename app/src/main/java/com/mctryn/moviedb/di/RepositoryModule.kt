package com.mctryn.moviedb.di

import org.koin.dsl.module

/**
 * Repository module for dependency injection.
 * 
 * TODO: Phase 2 - Implement repositories
 * - Create MovieRepository interface
 * - Create MovieRepositoryImpl
 */
val repositoryModule = module {
    // TODO: Add repository providers
    // single<MovieRepository> { MovieRepositoryImpl(get(), get(), get()) }
}